# ══════════════════════════════════════════════════════
# Gemini Client — 환경 변수 설정 예시
# .env 파일로 복사 후 실제 값으로 수정하세요:
#   cp .env.example .env
#
# ※ Android APK는 앱 내부 설정 화면에서 직접 입력합니다.
#    이 파일은 Mac/PC의 gemini_client.py / server.py 전용입니다.
# ══════════════════════════════════════════════════════

# ── Gemini API ─────────────────────────────────────────
# Google AI Studio에서 발급: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ── Gmail SMTP ─────────────────────────────────────────
# 발신자 Gmail 주소
GMAIL_SENDER_EMAIL=your_sender@gmail.com

# Gmail 앱 비밀번호 (계정 비밀번호 X, 앱 전용 16자리)
# 발급: https://myaccount.google.com/apppasswords
# (Google 계정 2단계 인증 활성화 필수)
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# 수신자 이메일
GMAIL_RECEIVER_EMAIL=your_receiver@example.com

# ── 디바이스 프로파일 (Mac gemini_client.py 전용) ────────
# iphone13 | pixel5 | galaxy_s24  (기본값: pixel5)
DEVICE=pixel5

# ── 브라우저 설정 (Mac gemini_client.py 전용) ────────────
# 디버깅: false / 백그라운드 자동화: true
HEADLESS=false

# ── 타임아웃 설정 (ms, Mac gemini_client.py 전용) ────────
TIMEOUT_PAGE_LOAD=30000
TIMEOUT_INPUT_READY=20000
TIMEOUT_LOGIN_WAIT=120000
TIMEOUT_STOP_APPEAR=15000
TIMEOUT_STOP_GONE=180000
TIMEOUT_RESPONSE_MAX=180000

# ── 재시도 횟수 (Mac gemini_client.py 전용) ──────────────
MAX_NAV_RETRIES=3
MAX_SEND_RETRIES=2

# ── Flask API 서버 설정 (server.py 실행 시) ────────────
SERVER_HOST=0.0.0.0
SERVER_PORT=5000
